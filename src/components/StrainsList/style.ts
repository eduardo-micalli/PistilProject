import styled from 'styled-components';

export const CardList = styled.div`
    display: inline-flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-evenly;
    margin-top: 40px;
    padding: 10px;
`;
